/* provider "aws" {
  alias = "cluster-broker"

  region  = var.region

  assume_role {
    role_arn = var.role_arn.cluster-broker
  }
} */
/* 
terraform {
  required_providers {
    aws = {
      version = "~> 3.42.0"
    }
} */
