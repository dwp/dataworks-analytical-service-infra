groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
      - preprod
      - production
  - name: pull-request
    jobs:
      - dataworks-analytical-service-infra-pr
